import React, { useEffect } from "react";
import Axios from "axios";

export const Order = ({ user }) => {
  const getOrders = () => {
    console.log(user);
  };

  console.log(user);
  return <div>Order</div>;
};
